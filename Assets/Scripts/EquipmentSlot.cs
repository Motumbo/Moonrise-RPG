using UnityEngine;
using System.Collections;

public class EquipmentSlot : MonoBehaviour {//cuando equipo un item si el slot tiene este script lo sumo a los stats del pj

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
